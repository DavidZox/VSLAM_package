# Third-party upstream clones (each has its own git history; re-clone via the commands in README/notes)
/g2o/
/stella_vslam/
/stella_vslam_ros/
/stella_vslam_examples/

# Local from-source install prefix (regenerated by building g2o/stella_vslam)
/local_install/

# Downloaded runtime assets (re-downloadable, not project source)
/vocab/

# Build logs
*_build.log

# Generic build directories
build/
